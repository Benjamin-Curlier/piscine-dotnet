# Retex — v5 S14 : docs recrue + déploiement (terminal in-app, résultat riche, installeurs)

> Issue #47. Branche `v5/s14-docs`. **Dernier sprint v5.**
> **Verdict : objectif atteint.** Les docs recrue/mainteneur reflètent l'état final v5 : **terminal
> embarqué + coaching git** (S12), **résultat de push riche** (#40/S11), **installeurs Windows + Linux
> offline/online + macOS abandonné + webkit2gtk-4.0** (S13). Docs uniquement (+ CHANGELOG/Curriculum) ;
> `validate-content` vert ; **aucun `src/`, aucun tag.**

## Ce qui a été fait
- **`docs/mise-en-oeuvre.md`** (réécrit) : plateformes **Windows + Linux** (macOS retiré) ; installation
  par **installeur** (Windows `.exe` per-utilisateur, Linux `.AppImage`) **ou zip**, chacun en **offline/
  online** ; webview géré par l'installeur (offline embarqué / online téléchargé) ou manuel en zip
  (**`libwebkit2gtk-4.0`**, pas 4.1) ; l'app **embarque un terminal + coaching git** (page *Terminal*) →
  le `git push` du rendu peut s'y faire (ou au terminal système ; AppImage Linux → préférer le système) ;
  *Vérifier* et *Résultat* décrits en **riche** (diff/indice/lien cours). Check-list encadrant MAJ.
- **`docs/deploiement.md`** : §4 réécrit en **3 jobs** (`package-linux` zips+AppImage / `package-windows`
  installeurs Inno / `release` agrège) ; artefacts nommés ; prérequis webview **4.0** + géré par installeur ;
  §5 dry-runs CI (desktop + **AppImage offline lancé hors-ligne** + installeur Windows compilé) ; **smoke
  pré-release** MAJ (installeurs offline/online, **terminal+coaching**, *Résultat* riche ; macOS retiré).
- **`CHANGELOG.md`** (`[Non publié]`) : terminal+coaching et résultat riche **passés en « Ajouté »**
  (n'étaient plus « différés ») ; section packaging = installeurs + zips, webkit-4.0 ; « Limites connues »
  = macOS retiré + caveat push AppImage (terminal système pour le hook).
- **`docs/wiki/Curriculum.md`** + **`docs/wiki/Mise-en-oeuvre.md`** : note UX alignée (terminal+coaching,
  résultat riche, installeurs, macOS retiré).

## Prouvé
- **`validate-content` → « Contenu valide. »** (gate verte).
- **Honnêteté** : seul l'état livré+vérifié est décrit. Le terminal+coaching est présenté comme
  disponible (livré S12, prouvé Windows + Linux Docker) ; la **confirmation visuelle fenêtre native**
  reste une étape **proprio** (smoke pré-release). Le `git push` au **terminal système** reste le chemin
  universellement valide (caveat AppImage explicité).
- **Garde** : aucun fichier `src/` ni `.github/` touché (docs + CHANGELOG seulement). Aucun tag.

## Décisions / limites
- **Retex/plans/ADR/specs historiques laissés tels quels** (records datés ; pas de réécriture
  rétroactive) — seules les docs **vivantes** (recrue/mainteneur/CHANGELOG/Curriculum/HANDOFF) sont MAJ.
- **CHANGELOG `[v1.0.0]`/`[v2.0.0]`** : entrées historiques inchangées (osx-arm64 était distribué alors —
  exact pour ces versions). Le retrait macOS ne concerne que le **non-publié**.

## Pièges réutilisables
- **Deux docs « mise en œuvre »** : la canonique `docs/mise-en-oeuvre.md` **et** un pointeur wiki
  `docs/wiki/Mise-en-oeuvre.md` (résumé). MAJ les deux pour éviter une divergence recrue.
- **Honnêteté docs vs QA proprio** : un feature *livré + testé* (terminal+coaching) se documente comme
  réel ; la *confirmation visuelle de la fenêtre native* (non automatisable) reste une case smoke, pas un
  blocage de documentation.
