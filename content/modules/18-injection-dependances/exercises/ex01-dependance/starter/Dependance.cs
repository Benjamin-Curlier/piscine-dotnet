// Lis n ; enregistre Multiplieur et Traitement, résous Traitement (qui reçoit Multiplieur par constructeur), affiche Traiter(n).
// Astuce : public Traitement(Multiplieur m) => _m = m; et Traiter(n) => _m.Doubler(n); avec Doubler(x) => x * 2.
