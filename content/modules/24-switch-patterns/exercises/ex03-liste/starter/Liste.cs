// Lis une ligne d'entiers separes par des espaces, parse en int[] et analyse-la
// avec un list pattern.
// Astuce : t switch { [var x] => "un seul", [var f, .., var l] => $"premier={f} dernier={l}", _ => "?" };
// Pense a using System.Linq; pour Select(int.Parse).ToArray().
