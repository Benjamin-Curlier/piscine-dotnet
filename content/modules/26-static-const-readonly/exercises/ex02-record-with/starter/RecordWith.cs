// Lis X puis Y, crée un record Point(X, Y), puis crée p2 = p with { X = X + 1 }.
// Affiche p2 au format (X, Y).
// Astuce : record Point(int X, int Y); à déclarer après le code principal.

// ... code principal ici ...

// Déclare le record Point après le code principal.
