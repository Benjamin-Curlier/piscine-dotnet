// Lis N, puis N entiers triés séparés par des espaces, puis une cible.
// Affiche l'indice 0-based de la cible dans le tableau, ou -1 si absente.
// Implémente la recherche binaire à la main (pas d'Array.BinarySearch).
// Astuce : gauche=0, droite=n-1 ; milieu=(gauche+droite)/2 ; compare t[milieu] à la cible.
