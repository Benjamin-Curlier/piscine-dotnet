// Lis N, puis N entiers séparés par des espaces. Trie le tableau par ordre croissant
// avec le tri fusion récursif (à la main). Affiche le résultat séparé par des espaces.
// Astuce : méthode TriFusion(int[] t) — cas de base si length<=1 ; sinon coupe, trie
//          chaque moitié récursivement, puis fusionne avec Fusionner(gauche, droite).
// Les méthodes locales static doivent être déclarées APRÈS le code principal.
