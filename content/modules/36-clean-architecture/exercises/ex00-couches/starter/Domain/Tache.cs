namespace Domain;

// Couche DOMAIN : l'entité métier. Aucune dépendance vers Application ni Infrastructure.
// TODO : id (int, lecture seule), titre (string, lecture seule), faite (bool), et Terminer().
public sealed class Tache
{
    public Tache(int id, string titre)
    {
        // TODO : initialiser Id et Titre.
    }

    // TODO : propriétés Id, Titre, Faite et méthode Terminer().
}
