namespace Domain;

// Couche DOMAIN : le SECOND port. Le domaine dit ce dont il a besoin (« prévenir ») ;
// l'infrastructure décidera comment.
public interface INotificateur
{
    // TODO : Notifier(string message).
}
