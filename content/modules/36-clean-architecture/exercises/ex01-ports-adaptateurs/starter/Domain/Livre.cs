namespace Domain;

// Couche DOMAIN : l'entité Livre. Logique métier pure, aucune dépendance technique.
public sealed class Livre
{
    // TODO : Id (int), Titre (string), Emprunte (bool, modifiable seulement via des méthodes).
    // TODO : Emprunter() met Emprunte à true ; Rendre() le remet à false.
}
