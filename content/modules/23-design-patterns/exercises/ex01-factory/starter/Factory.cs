// Lis N, puis N types ("chien"/"chat") ; via AnimalFactory.Creer(type), affiche le cri de chacun.
// Astuce : interface IAnimal { string Cri(); } puis static class AnimalFactory { static IAnimal Creer(...) }
