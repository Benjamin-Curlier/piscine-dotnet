// Lis deux entiers a et b (sur deux lignes). Deux "services" incrémentent le MÊME
// compteur partagé (un singleton) : a fois, puis b fois. Affiche la valeur finale.

var a = int.Parse(System.Console.ReadLine());
var b = int.Parse(System.Console.ReadLine());

// TODO : incrémente Compteur.Instance a fois puis b fois, puis affiche sa Valeur.

// TODO : écris la classe Compteur (constructeur privé + propriété statique Instance).
